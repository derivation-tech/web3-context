## Install Deps

```shell
pnpm deps
```

## Build

```shell
pnpm run build
```

## Add a new package

```shell
./scripts/init.sh packageName
```

## Publish

`cd` to the package you want to publish and run:

```shell
pnpm run publish
```
